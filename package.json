{
  "name": "titan-trading-system",
  "private": true,
  "description": "Titan Trading System Monorepo",
  "scripts": {
    "start": "echo 'Use service specific start command'",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
