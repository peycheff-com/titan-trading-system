{
  "port": 3100,
  "logLevel": "info",
  "database": {
    "host": "${TITAN_DB_HOST}",
    "port": "${TITAN_DB_PORT}",
    "name": "${TITAN_DB_NAME}",
    "user": "${TITAN_DB_USER}",
    "password": "${TITAN_DB_PASSWORD}",
    "ssl": false
  },
  "redis": {
    "url": "redis://localhost:6379/1",
    "keyPrefix": "titan:brain:"
  }
}